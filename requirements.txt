# Phase A (GPU 없음, CPU). Python 3.13
# 설치:  python -m venv .venv && .venv/bin/pip install -r requirements.txt

# 백엔드 / API
fastapi
uvicorn[standard]
python-multipart          # 파일 업로드(작품 등록)
python-dotenv
requests

# A1 AI
openai                    # K-EXAONE (Friendli, OpenAI 호환)
# VARCO(text2sound·image-to-3D)는 requests 로 직접 호출 — 추가 패키지 없음

# A2 RAG
chromadb                  # 벡터 검색 (기본 onnx 임베딩 사용 → torch 불필요)
# TODO(B3): BGE-Korean/KoSimCSE 임베딩(GPU)로 교체 시 sentence-transformers 추가

# A3 형태 파이프라인
numpy
pillow                    # 이미지 처리
rembg                     # 배경 제거(단일 오브젝트 추출)
onnxruntime               # rembg/chroma 런타임
trimesh                   # GLB 3D → 정면 깊이맵(H)
rtree                     # trimesh 레이캐스트 가속
